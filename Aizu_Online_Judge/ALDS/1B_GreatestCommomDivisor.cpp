#include <bits/stdc++.h>
using namespace std;

int eucrid(int a, int b){
  if (a )
}

int main(){
  int a, b, tmp;
  if(a < b){
    tmp = a;
    a = b;
    b = tmp;
  }
  int ans = eucrid(a, b);
  printf("%d", ans);
}
